<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%# TODO ナビバー作成 %>
    <div class="fixed-top d-flex border-bottom p-2">
      <%= link_to image_tag('logo.png', height: 28), '/'%>
      <div class="ms-auto">
        <% if user_signed_in? %>
          <%= link_to '投稿', new_meal_path %>
          <%= link_to 'ログアウト', destroy_user_session_path %>
        <% else%>
          <%= link_to '仮ユーザログイン', kari_login_path, method: :post %>
        <% end %>
        user_id: <%= user_signed_in? ? "#{current_user.id}" : '未ログイン' %>
      </div>
    </div>
    <main class="container pt-5">
      <%= yield %>
    </main>
  </body>
</html>
